<h1>Programs#new</h1>
<p>Find me in app/views/programs/new.html.erb</p>

<div class="container">
  <h1>Ajouter un programme</h1>
   <div class="grid">
    <div class="f-new">
      <ul class="new-form">
        <%= simple_form_for(@program) do |f| %>
          <!-- [...] -->
          <%= f.input :title%>
          <%= f.input :category_id, collection: @categories.collect{|category| [category.name, category.id]} %>
          <%= f.input :content %>
          <%= f.submit 'Créer', class: "btn-photo" %>
          <!-- [...] -->
        <% end %>
      </ul>
    </div>
    <%= link_to 'Annuler', programs_path, class:"btn btn-warning" %>
  </div>

</div>
