<div class="home-header">
    <h1 class="page_title blue-gray">Mettre à jour mon profil</h1>
</div>
<br>
<br>
<%= simple_form_for(resource, as: resource_name, url: registration_path(resource_name), html: { method: :put }) do |f| %>
  <%= f.error_notification %>

  <div class="col-md-4 col-md-offset-4">
    <div class="form-inputs">
      <%= f.input :first_name, :label=> "Prénom", required: true, autofocus: true %>
      <%= f.input :last_name, :label=> "Nom", required: true, autofocus: true %>
      <%= f.input :email, required: true, autofocus: true %>
      <%= f.input :photo, autofocus: true %>
      <% if devise_mapping.confirmable? && resource.pending_reconfirmation? %>
        <p>Currently waiting confirmation for: <%= resource.unconfirmed_email %></p>
      <% end %>

      <%= f.input :password, :label=> "Mot de passe", autocomplete: "off", hint: "Laissez blanc si vous ne voulez pas changer votre mot de passe", required: false %>
      <%= f.input :password_confirmation, :label=> "Confirmation mot de passe", required: false %>
      <%= f.input :current_password, :label=> "Votre mot de passe actuel", hint: "Nous avons besoin de votre mot de passe actuel pour prendre en compte les modifications", required: true %>
    </div>
    <div class="form-actions">
      <%= f.button :submit, "Mettre à jour", class: "btn-restart btn-orange centerize" %>
    </div>
<% end %>

  <!-- <div>
    <h3>Supprimer mon compte?</h3>
    <br>
    <br> -->
    <p><%#= link_to "Cancel my account", registration_path(resource_name), data: { confirm: "Are you sure?" }, method: :delete, class: "btn-restart btn-red"  %>
    <!-- </p> -->
  <!-- </div> -->
</div>
<%#= link_to "Back", :back %>
