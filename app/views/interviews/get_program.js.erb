console.log("<%= @answer.id %>");

function getProgram(program) {
document.getElementById("user_program_answer_id").value = "<%= @answer.id %>";
document.getElementById("user_program_program").value = "<%= j @content %>";
document.querySelector(".button-answer").classList.add("btn-red");
};

var answer = document.querySelector(".answer");
answer.addEventListener('click', getProgram());
